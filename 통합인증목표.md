# 목표

k3s 환경에 배포된 여러 웹 데모 프로젝트에 공통 데모 헤더와 통합 임시 로그인 기능을 구축한다.

현재 데모 프로젝트들은 서로 다른 기술 스택을 사용한다.

* A 프로젝트: React
* B 프로젝트: Next.js
* 추후 다른 웹 프로젝트 추가 가능

사용자는 `demo.yourdomain.com`으로만 진입하지 않아도 된다.
`a.yourdomain.com`, `b.yourdomain.com` 등 개별 프로젝트 URL로 직접 접속해도 동일한 상단 데모 헤더가 보여야 한다.

상단 헤더에는 다음 기능이 있어야 한다.

* 데모 프로젝트 이동 버튼
* 현재 로그인 상태 표시
* 임시 로그인 버튼
* 로그아웃 버튼

회원가입 없이 버튼 클릭만으로 임시 계정이 배정되고, 모든 데모 프로젝트에서 동일한 임시 로그인 상태를 공유해야 한다.

---

# 전체 구조

도메인 구조는 다음과 같이 가정한다.

```text
demo.yourdomain.com   → 데모 포털 / 인증 API
a.yourdomain.com      → A 프로젝트
b.yourdomain.com      → B 프로젝트
c.yourdomain.com      → C 프로젝트
```

`demo.yourdomain.com`은 필수 진입점이 아니라 랜딩 페이지 겸 인증 API 서버 역할을 한다.

사용자는 아래 경로 모두로 진입 가능해야 한다.

```text
demo.yourdomain.com
a.yourdomain.com
b.yourdomain.com
c.yourdomain.com
```

각 프로젝트에 동일한 `DemoHeader` 컴포넌트를 삽입한다.

---

# 인증 방식

Keycloak은 초기 단계에서는 사용하지 않는다.

이유:

* 현재 목표는 실제 회원 인증 시스템이 아니라 데모용 임시 로그인이다.
* Keycloak은 OIDC/OAuth2 기반의 완성형 인증 플랫폼이므로 운영 복잡도가 크다.
* 버튼 클릭만으로 tempUser를 자동 배정하는 UX는 Keycloak 기본 기능만으로는 매끄럽지 않다.
* Keycloak에서 버튼형 임시 로그인을 제대로 구현하려면 Custom Authenticator가 필요할 수 있다.
* 초기 목표에는 직접 만든 가벼운 demo-auth 기능이 더 적합하다.

따라서 초기 구현은 다음 방식으로 한다.

```text
demo-portal-backend
  ├── POST /api/demo-login
  ├── GET  /api/me
  ├── POST /api/logout
  └── GET  /api/auth/public-key 또는 JWT secret 기반 검증
```

처음에는 별도 `auth-service`로 분리하지 않고, `demo-portal-backend` 내부에 인증 API를 포함한다.

추후 프로젝트 수가 늘어나거나 인증 기능이 커지면 `demo-auth-service`로 분리할 수 있다.

---

# 로그인 흐름

사용자가 어떤 프로젝트에서든 `[임시 로그인]` 버튼을 누르면 다음 흐름으로 처리한다.

```text
1. 사용자가 a.yourdomain.com 접속
2. A 프로젝트의 DemoHeader 표시
3. DemoHeader가 GET https://demo.yourdomain.com/api/me 호출
4. 로그인 쿠키가 없으면 비로그인 상태 표시
5. 사용자가 [임시 로그인] 클릭
6. DemoHeader가 POST https://demo.yourdomain.com/api/demo-login 호출
7. demo-portal-backend가 tempUser를 생성 또는 배정
8. HttpOnly Cookie로 DEMO_TOKEN 발급
9. 브라우저는 .yourdomain.com 범위의 쿠키 저장
10. A/B/C 프로젝트 이동 시 동일 쿠키 공유
11. 각 프로젝트 백엔드는 DEMO_TOKEN을 검증하고 내부 demo user로 매핑
```

---

# 쿠키 정책

임시 로그인 토큰은 HttpOnly Cookie로 발급한다.

예시:

```http
Set-Cookie: DEMO_TOKEN=<jwt>;
HttpOnly;
Secure;
SameSite=Lax;
Domain=.yourdomain.com;
Path=/;
Max-Age=86400
```

주의사항:

* `Domain=.yourdomain.com`으로 설정해야 `a.yourdomain.com`, `b.yourdomain.com`, `demo.yourdomain.com`에서 공유 가능하다.
* `HttpOnly`로 설정하므로 프론트엔드 JavaScript에서는 쿠키를 직접 읽지 않는다.
* 로그인 상태 표시는 반드시 `/api/me` 호출 결과로 판단한다.
* HTTPS 환경을 전제로 `Secure`를 사용한다.

---

# CORS 설정

각 프로젝트 프론트에서 `demo.yourdomain.com`의 인증 API를 호출해야 하므로 CORS 설정이 필요하다.

허용 origin 예시:

```text
https://demo.yourdomain.com
https://a.yourdomain.com
https://b.yourdomain.com
https://c.yourdomain.com
```

credentials 사용이 필요하므로 다음 조건을 지켜야 한다.

```text
Access-Control-Allow-Credentials: true
Access-Control-Allow-Origin: * 사용 금지
```

프론트 fetch 예시:

```ts
fetch("https://demo.yourdomain.com/api/me", {
  credentials: "include",
});
```

---

# DemoHeader 요구사항

`DemoHeader`는 React와 Next.js에서 모두 사용할 수 있어야 한다.

초기에는 각 프로젝트에 같은 컴포넌트 코드를 복사해서 넣는다.
추후 프로젝트가 늘어나면 별도 npm 패키지 또는 monorepo package로 분리한다.

## 기능

`DemoHeader`는 다음 기능을 가진다.

```text
- 프로젝트 이동 링크 표시
- 현재 프로젝트 강조
- 로그인 상태 조회
- 임시 로그인 버튼
- 로그아웃 버튼
- 로그인 후 nickname 표시
```

예시 UI:

```text
[A Project] [B Project] [C Project]          비로그인 [임시 로그인]
```

로그인 후:

```text
[A Project] [B Project] [C Project]          tempUser21 [로그아웃]
```

## 프로젝트 링크 설정

공통 설정으로 관리한다.

```ts
const DEMO_PROJECTS = [
  {
    name: "Demo Home",
    url: "https://demo.yourdomain.com",
  },
  {
    name: "A Project",
    url: "https://a.yourdomain.com",
  },
  {
    name: "B Project",
    url: "https://b.yourdomain.com",
  },
];
```

---

# React 프로젝트 적용 위치

일반 React 프로젝트에서는 `App.tsx` 또는 라우터 바깥에 삽입한다.

예시:

```tsx
import DemoHeader from "./components/DemoHeader";

function App() {
  return (
    <>
      <DemoHeader />
      <Routes />
    </>
  );
}

export default App;
```

또는:

```tsx
<BrowserRouter>
  <DemoHeader />
  <AppRoutes />
</BrowserRouter>
```

---

# Next.js 프로젝트 적용 위치

Next.js App Router를 사용한다면 `DemoHeader`는 클라이언트 컴포넌트여야 한다.

```tsx
"use client";

export function DemoHeader() {
  // useEffect, fetch, window.location 사용 가능
}
```

`app/layout.tsx`에 삽입한다.

```tsx
import { DemoHeader } from "@/components/DemoHeader";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ko">
      <body>
        <DemoHeader />
        {children}
      </body>
    </html>
  );
}
```

Pages Router를 사용한다면 `_app.tsx`에 삽입한다.

```tsx
import DemoHeader from "@/components/DemoHeader";

export default function App({ Component, pageProps }) {
  return (
    <>
      <DemoHeader />
      <Component {...pageProps} />
    </>
  );
}
```

---

# DemoHeader API 호출 방식

## 로그인 상태 조회

```ts
async function fetchMe() {
  const res = await fetch("https://demo.yourdomain.com/api/me", {
    credentials: "include",
  });

  if (!res.ok) {
    return null;
  }

  return res.json();
}
```

응답 예시:

```json
{
  "loggedIn": true,
  "user": {
    "id": "temp-user-21",
    "nickname": "tempUser21",
    "role": "DEMO"
  }
}
```

비로그인 응답 예시:

```json
{
  "loggedIn": false,
  "user": null
}
```

## 임시 로그인

```ts
async function demoLogin() {
  const res = await fetch("https://demo.yourdomain.com/api/demo-login", {
    method: "POST",
    credentials: "include",
  });

  if (!res.ok) {
    throw new Error("demo login failed");
  }

  return res.json();
}
```

응답 예시:

```json
{
  "user": {
    "id": "temp-user-21",
    "nickname": "tempUser21",
    "role": "DEMO"
  }
}
```

## 로그아웃

```ts
async function logout() {
  await fetch("https://demo.yourdomain.com/api/logout", {
    method: "POST",
    credentials: "include",
  });
}
```

로그아웃 시 서버는 쿠키를 만료시킨다.

```http
Set-Cookie: DEMO_TOKEN=;
HttpOnly;
Secure;
SameSite=Lax;
Domain=.yourdomain.com;
Path=/;
Max-Age=0
```

---

# demo-portal-backend 요구사항

## POST /api/demo-login

역할:

```text
- 임시 사용자 생성 또는 배정
- DEMO role 부여
- JWT 생성
- HttpOnly Cookie 발급
- 사용자 정보 반환
```

초기 구현은 DB 없이 가능하다.

선택지:

```text
1. JWT에 temp user 정보를 직접 포함
2. Redis에 temp user 세션 저장
3. DB에 temp user 저장
```

초기 추천:

```text
JWT 기반 stateless 방식
```

JWT payload 예시:

```json
{
  "sub": "temp-user-21",
  "nickname": "tempUser21",
  "role": "DEMO",
  "type": "DEMO_USER",
  "iat": 1710000000,
  "exp": 1710086400
}
```

## GET /api/me

역할:

```text
- DEMO_TOKEN 쿠키 확인
- JWT 검증
- 유효하면 사용자 정보 반환
- 없거나 만료되었으면 loggedIn=false 반환
```

## POST /api/logout

역할:

```text
- DEMO_TOKEN 쿠키 만료
```

---

# 각 프로젝트 백엔드 요구사항

각 프로젝트 백엔드는 기존 회원가입/로그인 기능을 유지할 수 있다.
단, 데모 환경에서는 `DEMO_TOKEN` 쿠키를 검증하는 인증 미들웨어를 추가한다.

요청 처리 흐름:

```text
1. 요청 수신
2. DEMO_TOKEN 쿠키 확인
3. JWT 서명 검증
4. role=DEMO 확인
5. 내부 프로젝트 User와 매핑
6. 기존 회원 기능을 demo user 권한으로 수행
```

---

# 내부 사용자 매핑 전략

프로젝트마다 User 테이블 구조가 다를 수 있으므로, 초기에는 각 프로젝트 DB에 demo user를 미리 만들어둔다.

예시:

```text
A 프로젝트 DB:
  user_id = 100
  nickname = tempUser

B 프로젝트 DB:
  user_id = demo
  nickname = tempUser
```

매핑 방식은 프로젝트별로 단순하게 시작한다.

예시:

```text
DEMO_TOKEN의 role이 DEMO이면
각 프로젝트 내부의 고정 demo user로 처리
```

초기에는 tempUser별로 내부 사용자를 매번 생성하지 않아도 된다.

추천 초기 방식:

```text
모든 임시 사용자를 각 프로젝트 내부의 단일 demo user로 매핑
```

추후 개선 방식:

```text
temp-user-21 → A 프로젝트 내부 user 121
temp-user-21 → B 프로젝트 내부 user 45
```

---

# DEMO role 제한

데모 사용자는 모든 기능을 사용할 수 있으면 안 된다.

DEMO role에서 제한해야 할 기능:

```text
- 비밀번호 변경
- 이메일 변경
- 회원 탈퇴
- 관리자 기능
- 결제/주문/실제 알림 발송
- 외부 API 호출
- 파일 대량 업로드
- 다른 사용자 개인정보 조회
```

일반적인 회원 기능은 체험 가능하게 둔다.

허용 예시:

```text
- 게시글 작성
- 댓글 작성
- 좋아요
- 문제 제출
- 상품 조회
- 개인 설정 일부 변경
```

---

# 데이터 초기화 전략

데모 계정이 만든 데이터는 주기적으로 초기화해야 한다.

초기 구현에서는 Kubernetes CronJob 또는 각 프로젝트의 배치 작업으로 처리한다.

예시 정책:

```text
- 매일 새벽 4시 데모 데이터 삭제
- demo user가 작성한 게시글/댓글/제출 기록 삭제
- 업로드 파일 삭제
- 기본 샘플 데이터 유지
```

k3s CronJob 예시:

```yaml
apiVersion: batch/v1
kind: CronJob
metadata:
  name: demo-cleanup
spec:
  schedule: "0 4 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: cleanup
              image: demo-cleanup:latest
          restartPolicy: OnFailure
```

초기에는 각 프로젝트 DB별로 cleanup script를 따로 작성해도 된다.

---

# k3s Ingress 구조

예시:

```text
Ingress
  ├── demo.yourdomain.com → demo-portal-service
  ├── a.yourdomain.com    → a-project-service
  ├── b.yourdomain.com    → b-project-service
  └── c.yourdomain.com    → c-project-service
```

인증 API가 `demo-portal-backend`에 포함되어 있다면:

```text
https://demo.yourdomain.com/api/demo-login
https://demo.yourdomain.com/api/me
https://demo.yourdomain.com/api/logout
```

각 프로젝트는 위 API를 호출한다.

---

# CSS 및 UI 주의사항

공통 헤더는 각 프로젝트의 CSS와 충돌하지 않아야 한다.

권장:

```text
- CSS Module 사용
- className prefix 사용
- 전역 스타일 의존 최소화
- z-index 명확히 지정
- height 고정
```

예시 class prefix:

```css
.demo-header {}
.demo-header__nav {}
.demo-header__link {}
.demo-header__auth {}
.demo-header__button {}
```

피해야 할 방식:

```text
- Nginx/Ingress에서 HTML에 헤더 강제 삽입
- iframe으로 공통 헤더 삽입
```

이유:

```text
- React/Next.js hydration 문제 가능
- CSS 충돌 가능
- 라우팅 문제 가능
- 유지보수 어려움
```

---

# 구현 우선순위

## 1단계

```text
- demo-portal-backend에 /api/demo-login, /api/me, /api/logout 구현
- HttpOnly Cookie 발급
- CORS 설정
```

## 2단계

```text
- React용 DemoHeader 구현
- A 프로젝트에 삽입
- 로그인 상태 조회, 임시 로그인, 로그아웃 동작 확인
```

## 3단계

```text
- Next.js용 DemoHeader 구현
- B 프로젝트 layout 또는 _app에 삽입
- 동일 동작 확인
```

## 4단계

```text
- 각 프로젝트 백엔드에서 DEMO_TOKEN 검증 미들웨어 추가
- 내부 demo user 매핑
- 회원 기능 체험 가능하게 수정
```

## 5단계

```text
- DEMO role 제한 추가
- 위험 기능 차단
```

## 6단계

```text
- 데모 데이터 cleanup batch 또는 k3s CronJob 추가
```

## 7단계

```text
- 프로젝트가 늘어나면 DemoHeader를 공통 npm package로 분리
```

---

# 최종 목표 상태

최종적으로 다음이 가능해야 한다.

```text
1. 사용자가 a.yourdomain.com으로 직접 접속한다.
2. A 프로젝트 상단에 공통 DemoHeader가 보인다.
3. 사용자가 [임시 로그인] 버튼을 클릭한다.
4. tempUser 계정이 자동 배정된다.
5. 사용자는 별도 회원가입이나 ID/PW 입력 없이 A 프로젝트의 회원 기능을 체험한다.
6. 사용자가 B 프로젝트 버튼을 클릭한다.
7. b.yourdomain.com으로 이동한다.
8. B 프로젝트에서도 동일한 tempUser 로그인 상태가 유지된다.
9. B 프로젝트의 회원 기능도 체험 가능하다.
10. 로그아웃 버튼을 누르면 모든 데모 프로젝트에서 로그아웃된다.
```

---

# 설계 요약

```text
공통 헤더:
각 프로젝트 프론트에 삽입

통합 로그인:
demo-portal-backend에서 버튼형 임시 로그인 제공

로그인 공유:
.yourdomain.com 범위의 HttpOnly Cookie 사용

프로젝트 간 인증:
각 프로젝트 백엔드가 DEMO_TOKEN JWT 검증

사용자 매핑:
초기에는 각 프로젝트 내부 고정 demo user로 매핑

운영 안정성:
DEMO role 제한 + 주기적 데이터 초기화

Keycloak:
초기에는 사용하지 않음
추후 실제 SSO/OIDC가 필요해지면 도입 검토
```
